# Architecture

Wasabi is a static React application organized around UI components, domain models, pure analysis services, browser storage, and export adapters. No remote backend is required by the MVP.
