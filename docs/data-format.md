# Wasabi project format

The planned `.wasabi.json` envelope is represented by `ExportedWasabiProject` and contains version metadata, a TEMAC project, search strategy, criteria, clusters, and export timestamp.
