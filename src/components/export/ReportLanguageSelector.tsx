import { useTranslation } from 'react-i18next'; import type { SupportedLanguage } from '../../domain/language';
export function ReportLanguageSelector({ value, onChange }: { value: SupportedLanguage; onChange: (language: SupportedLanguage) => void }) { const { t } = useTranslation(); return <label className="grid gap-1 text-sm font-semibold text-slate-700"><span>{t('export.reportLanguage')}</span><select value={value} onChange={(event) => onChange(event.target.value as SupportedLanguage)} className="rounded-lg border border-slate-200 px-3 py-2"><option value="pt-BR">{t('language.portuguese')}</option><option value="en-US">{t('language.english')}</option></select></label>; }
