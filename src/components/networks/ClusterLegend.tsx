import { useTranslation } from 'react-i18next'; import type { ClusterSummary } from '../../domain/networkTypes';
export function ClusterLegend({ clusters }: { clusters: ClusterSummary[] }) { const { t } = useTranslation(); return <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{clusters.map((cluster) => <article key={cluster.id} className="rounded-xl border border-slate-200 bg-white p-4"><p className="text-sm font-bold text-wasabi-700">{t('networks.clusterNumber', { number: cluster.id })}</p><h4 className="mt-1 font-semibold text-slate-800">{cluster.label}</h4><p className="mt-2 text-sm text-slate-500">{t('networks.nodesCount', { count: cluster.nodeIds.length })}</p></article>)}</div>; }
